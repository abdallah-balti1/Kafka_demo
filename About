from sqlalchemy.orm import selectinload

def get_all(self) -> List[User]:
    return (
        self.session.query(User)
        .options(selectinload(User.pocs).selectinload(UserPocRelation.poc))
        .all()
    )
